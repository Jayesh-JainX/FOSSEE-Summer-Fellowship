---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="description" content="Astro description">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="/favicon.png">
    <meta name="generator" content={Astro.generator}>
    <title>{title}</title>
    <!-- Import any global stylesheets here -->
	<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <slot />
</body>

</html>

<style is:global>

    /* Global styles */
    :root {
        --primary-color: #2b8a3e;
        --secondary-color: #48a868;
        --text-color: #333;
        --background-color: #f4f7f6;
        --background-gradient: linear-gradient(
            135deg,
            var(--secondary-color),
            var(--primary-color)
        );
        --font-family: 'Arial', sans-serif;
    }

    /* HTML and Body */
    html, body {
        margin: 0;
        padding: 0;
        font-family: var(--font-family);
		font-family: 'Poppins', sans-serif;
        color: var(--text-color);
        background: var(--background-gradient);
        min-height: 100vh;
        overflow-x: hidden;
    }

    /* Adjust the layout to be mobile-friendly */
    

	@media (min-width: 768px) {
		body{
			padding-top:30px;
		}
	}
        /* Navbar link styles */
        .nav-link, .nav-link-mobile {
            text-decoration: none;
            color: #007bff;
            transition: color 0.3s ease, background-color 0.3s ease;
            padding: 5px 10px;
            border-radius: 5px;
        }

        .nav-link:hover, .nav-link-mobile:hover {
            color: #fff;
            background-color: #007bff;
        }

        /* Header hover effect */
        h2:hover, h3:hover {
            color: #0056b3;
        }


		#contact{
			justify-content: center;
			
		}

        /* Core Team and Contact Us section with 2 columns for desktop and 1 column for mobile */
        

        /* Styling for core team members */
        #contact ul li {
            border: 3px solid #ccc;
            padding: 15px;
            border-radius: 10px;
        }

        /* Add styles to emphasize text */
        strong {
            font-weight: bold;
        }

        em {
            font-style: italic;
        }

        /* Increase padding and add hover effect to buttons */
        button {
            transition: background-color 0.3s ease;
            padding: 12px 25px;
        }

        button:hover {
            background-color: #0056b3;
        }

        /* Scrollbar customization */
        ::-webkit-scrollbar {
            width: 12px; /* Width of the scrollbar */
            height: 12px; /* Height of the scrollbar */
        }

        ::-webkit-scrollbar-track {
            background-color: #f4f4f4; /* Color of the scrollbar track */
            border-radius: 10px; /* Rounded corners */
        }

        ::-webkit-scrollbar-thumb {
            background-color: #007bff; /* Color of the scrollbar thumb */
            border-radius: 10px; /* Rounded corners */
            border: 3px solid #f4f4f4; /* Add padding to the thumb */
        }

        ::-webkit-scrollbar-thumb:hover {
            background-color: #0056b3; /* Darker color on hover */
        }

        ::-webkit-scrollbar-corner {
            background-color: #f4f4f4; /* Background color for the corner */
        }

		


       

        /* Core Team and Contact Us section with 2 columns for desktop and 1 column for mobile */
        #contact ul {
            display: grid;
			margin-left: 18%;
			margin-right: 18%;
			text-align:center
            grid-template-columns: repeat(1, 1fr);
            gap: 20px;
        }

        @media (max-width: 768px) {
            #contact ul {
				text-align: left;
				margin-left: 0px;
			margin-right: 0px;
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* Styling for core team members */
        #contact ul li {
            border: 3px solid #ccc;
            padding: 15px;
            border-radius: 10px;
        }

        /* Add styles to emphasize text */
        strong {
            font-weight: bold;
        }

        em {
            font-style: italic;
        }

        /* Increase padding and add hover effect to buttons */
        button {
            transition: background-color 0.3s ease;
            padding: 12px 25px;
        }

        button:hover {
            background-color: #0056b3;
        }
    
        /* Navbar link styles */
        .nav-link, .nav-link-mobile {
            text-decoration: none;
            color: #007bff;
            transition: color 0.3s ease, background-color 0.3s ease;
            padding: 10px 20px; /* Increased padding for better spacing */
            border-radius: 5px;
            text-align: center; /* Center the text in mobile mode */
            display: block; /* Ensure each link takes up the full width */
            margin: 10px 0; /* Add margin for spacing between links */
        }

        /* Navbar link hover effect */
        .nav-link:hover, .nav-link-mobile:hover {
            color: #fff;
            background-color: #007bff;
        }

        /* Header hover effect */
        h2:hover, h3:hover {
            color: #0056b3;
        }

        /* Core Team and Contact Us section with 2 columns for desktop and 1 column for mobile */
        #contact ul {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 20px;
        }


        /* Styling for core team members */
        #contact ul li {
            border: 3px solid #ccc;
            padding: 15px;
            border-radius: 10px;
        }

        /* Add styles to emphasize text */
        strong {
            font-weight: bold;
        }
/* Register button styling */
.register-button {
    display: block;
    padding: 15px 30px; /* Increase the padding for a larger button */
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    position: relative; /* Enable absolute positioning for the animation effect */
    overflow: hidden; /* Hide the overflow of the animation */
    font-size: 1.2rem; /* Increase font size */
    font-weight: bold; /* Make the button text bold */
    transition: background-color 0.3s ease;
}

/* Register button hover effect */
.register-button:hover {
    background-color: #0056b3;
}

/* Animation effect on hover */
.register-button:hover::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 123, 255, 0.5), transparent);
    animation: pass 1s infinite;
}

/* Animation keyframes */
@keyframes pass {
    0% {
        left: -100%;
    }
    100% {
        left: 100%;
    }
}

    /* Add any other global styles here */
</style>


<!-- Additional styles for mobile compatibility -->
<style is:global>
    /* Media query for mobile compatibility */
    @media (max-width: 768px) {
        /* Style adjustments for mobile */
        section {
            flex-direction: column;
            gap: 10px; /* Reduce gap for mobile */
            padding: 10px; /* Reduce padding for mobile */
        }
        /* Adjust image styles */
        section img {
            width: 100%;
            height: auto;
            border-radius: 10px;
        }
    }

	/* Register button styling */
.register-button {
    display: block;
    padding: 15px 30px; /* Increase the padding for a larger button */
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    position: relative; /* Enable absolute positioning for the animation effect */
    overflow: hidden; /* Hide the overflow of the animation */
    font-size: 1.2rem; /* Increase font size */
    font-weight: bold; /* Make the button text bold */
    transition: background-color 0.3s ease;
}

/* Register button hover effect */
.register-button:hover {
    background-color: #0056b3;
}

/* Animation effect on hover */
.register-button:hover::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 123, 255, 0.5), transparent);
    animation: pass 1s infinite;
}

/* Animation keyframes */
@keyframes pass {
    0% {
        left: -100%;
    }
    100% {
        left: 100%;
    }
}

/* Button container styles */
.button-container {
    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 15px; /* Add margin-top for spacing if needed */
}

/* Register button styling */
.register-button {
    display: block;
    padding: 15px 30px; /* Increase the padding for a larger button */
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    position: relative; /* Enable absolute positioning for the animation effect */
    overflow: hidden; /* Hide the overflow of the animation */
    font-size: 1.2rem; /* Increase font size */
    font-weight: bold; /* Make the button text bold */
    transition: background-color 0.3s ease;
}

/* Register button hover effect */
.register-button:hover {
    background-color: #0056b3;
}

/* Animation effect on hover */
.register-button:hover::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(0, 123, 255, 0.5), transparent);
    animation: pass 1s infinite;
}

/* Animation keyframes */
@keyframes pass {
    0% {
        left: -100%;
    }
    100% {
        left: 100%;
    }
}



/* Footer styling */
#footer {
    background-color: #007bff; /* Blue background */
    color: white; /* White text color */
    padding: 20px; /* Padding around the content */
    text-align: left; /* Left-align the text */
}

/* Footer container styling */
.footer-container {
    display: flex; /* Use flexbox layout */
    justify-content: space-around; /* Evenly distribute space between columns */
    flex-wrap: wrap; /* Wrap the content to handle smaller screen sizes */
}

/* Footer column styling */
.footer-column {
    flex: 1; /* Equal width for each column */
    padding: 10px; /* Padding for each column */
}

.footer-column ul {
    list-style-type: none; /* Remove list styling */
    padding: 0;
}

/* Apply styles to list items */
.footer-column ul li {
    margin: 5px 0; /* Vertical margin between list items */
}

/* Increase the padding and add hover effect for links in the footer */
.footer-column a {
    padding: 10px 0;
    color: white;
    text-decoration: none;
    transition: color 0.3s ease;
}

/* Link hover effect */
.footer-column a:hover {
    color: #d1eaff;
}

</style>
